# SPDX-License-Identifier: LGPL-2.1-or-later
# D-Bus session activation: start platformd-secretd (via systemd) the first time
# a client asks for org.freedesktop.secrets.
[D-BUS Service]
Name=org.freedesktop.secrets
Exec=@LIBEXECDIR@/platformd-secretd
SystemdService=platformd-secretd.service
